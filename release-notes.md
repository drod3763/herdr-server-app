Bundled, resident parent for `herdr server` so macOS can grant it Local Network and TCC
consent. See the README for why and how.

**Upgrading?** The Local Network grant is bound to this build's signature. After replacing
a granted install, re-grant "Herdr Server" in System Settings › Privacy & Security ›
Local Network.
